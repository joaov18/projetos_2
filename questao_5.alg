algoritmo "semnome"

var

num,soma,index,ressoma:inteiro
media:real
inicio

para index de 1 ate 5 faca
   leia(num)
   soma     <- (num + num)
   ressoma  <- (soma + num)
   media    <- (ressoma)/2
fimpara

escreval("Soma: ",ressoma)
escreval("Media: ",media)


fimalgoritmo
